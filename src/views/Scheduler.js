import React from "react";
import Sidebar from "../components/Layout/Sidebar";

const Scheduler = () => {
  return (
    <div id="main-content">
      <Sidebar />
      <div className="main-headings text-center">Scheduler</div>
    </div>
  );
};

export default Scheduler;
